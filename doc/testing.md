# План тестирования

## 1. Цели тестирования

- Проверить корректность протокола сообщений и его устойчивость к ошибкам входных данных.
- Проверить корректность CLI (аргументы запуска, режимы listen/connect).
- Проверить базовый сценарий peer-to-peer: отправка/приём сообщений между двумя узлами.
- Обеспечить автоматический прогон основного набора тестов в CI.

## 2. Объекты тестирования

- Парсер аргументов (`--name`, `--peer`, `--listen`) и валидация.
- Кодек протокола: сериализация/десериализация, лимиты.
- Транспорт TCP: подключение, чтение/запись, разрыв соединения.
- Логика ядра: маршрутизация сообщений, жизненный цикл соединений.

## 3. Уровни тестов

### 3.1 Unit-тесты

- `ArgsParser`:
  - имя обязательно;
  - корректный/некорректный `host:port`;
  - поведение при одновременных флагах.
- `MessageCodec`:
  - encode/decode round-trip;
  - невалидный JSON;
  - отсутствующие поля;
  - лимит длины `text`.

### 3.2 Интеграционные тесты

- «два узла на localhost»:
  - узел A слушает порт;
  - узел B подключается;
  - B отправляет сообщение → A отображает;
  - A отправляет ответ → B отображает.

### 3.3 Системные/ручные проверки

- Проверка интерактивного ввода в консоли.
- Проверка читаемости вывода (формат времени, имя отправителя).
- Проверка на Windows и Linux.

## 4. Запуск тестов

- `mvn -B -ntp test`

## 5. Чеклист тестов

- [x] CLI: `--help` работает без `--name` (ошибка с usage/help текстом)
- [x] CLI: `--name` обязателен
- [x] CLI: парсинг `--listen` и `--peer`
- [x] CLI: валидация плохого `host:port`
- [x] Protocol: encode/decode round-trip для `chat`
- [x] Protocol: невалидный JSON отклоняется
- [x] Protocol: неподдерживаемый `type` отклоняется
- [x] Protocol: превышение лимита `text` отклоняется
- [x] Transport: TCP-клиент может подключиться к TCP-серверу и передать строку
- [ ] Integration: два полноценных процесса/узла на localhost (stdin/stdout сценарий)
- [ ] Chat Core: тесты на формат отображения (sender/time/text) без реального сокета
- [ ] Transport: таймауты/разрывы соединения и корректная обработка
- [ ] Protocol: пропущенные поля/пустые поля/слишком длинная входящая строка
 - [x] Integration: два полноценных процесса/узла на localhost (stdin/stdout сценарий)
 - [x] Chat Core: тесты на формат отображения (sender/time/text) без реального сокета
 - [x] Transport: таймауты/разрывы соединения и корректная обработка
 - [x] Protocol: пропущенные поля/пустые поля/слишком длинная входящая строка
